name: release_cocoapod

on:
  push:
    tags:
      - 'v*.*.*'
      
  workflow_dispatch:
jobs:
  build:
    runs-on: macOS-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          persist-credentials: false 
          fetch-depth: 0 
        
      - name: Set env
        run: |
          echo "RELEASE_VERSION=${GITHUB_REF#refs/*/}" >> $GITHUB_ENV
      - name: Test
        run: |
          echo "Rel : ${{ env.RELEASE_VERSION }}  "
      - name: Update framework file to repo
        run: |
          git config --global user.email "amit15885@gmail.com"
          git config --global user.name "${{ secrets.GH_USERNAME }}"
          git tag -d ${{ env.RELEASE_VERSION }}  
          RELEASE_VERSION_STRIPPED= ${{ env.RELEASE_VERSION }} | sed 's/v//'
          git tag $RELEASE_VERSION_STRIPPED
        shell: bash
        
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GH_TOKEN }}
          branch: main
          tags: true
          force: true
      
